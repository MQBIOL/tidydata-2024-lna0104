# Calculate A^2: matrix multiplication using  %*%
squaredAdjMatrix <- adjMatrix %*% adjMatrix
squaredAdjMatrix <- squaredAdjMatrix %>%
as.data.frame() %>%
mutate(V1 = row.names(.)) %>%
`colnames<-`(row.names(.))
View(squaredAdjMatrix)
# Calculate A^2: matrix multiplication using  %*%
squaredAdjMatrix <- adjMatrix %*% adjMatrix
squaredAdjMatrixData <- squaredAdjMatrix %>%
as.data.frame() %>%
mutate(V1 = row.names(.)) %>%
`colnames<-`(row.names(.))
View(squaredAdjMatrixData)
# Calculate A^2: matrix multiplication using  %*%
squaredAdjMatrix <- adjMatrix %*% adjMatrix
squaredAdjMatrix <- squaredAdjMatrix %>%
as.data.frame() %>%
`colnames<-`(row.names(.)) %>%
mutate(V1 = row.names(.))
View(squaredAdjMatrix)
squaredAdjList <- squaredAdjMatrix %>%
pivot_longer(-V1, names_to = "V2", values_to = "weight")
View(squaredAdjList)
squaredAdjGraph <- graph_from_data_frame(squaredAdjList, directed = FALSE)
### Task 3.4:
# Create a graph
A <- make_graph(edges = c(1, 2, 1, 3, 1, 4), n = 4, directed = FALSE)
E(A)$weight <- c(1,1,1)
summary(A)
plot(A, edge.width = E(A)$weight)
#Generate adjacency matrix
adjMatrixA <- as_adjacency_matrix(A, sparse = FALSE,attr = "weight")
# Calculate A^2: matrix multiplication using  %*%
squaredAdjMatrixA <- adjMatrixA %*% adjMatrixA
print("Adjacency Matrix A:")
print(adjMatrixA)
print("\nAdjacency Matrix A^2:")
print(squaredAdjMatrixA)
squaredAdjMatrixA <- squaredAdjMatrixA %>%
as.data.frame() %>%
`colnames<-`(row.names(.)) %>%
mutate(V1 = row.names(.))
squaredAdjListA <- squaredAdjMatrixA %>%
pivot_longer(-V1, names_to = "V2", values_to = "weight")
squaredGraphA <- graph_from_data_frame(squaredAdjListA, directed = FALSE)
plot(squaredGraphA, edge.width = E(squaredGraphA)$weight)
E(squaredGraphA)$weight
setwd("~/Desktop/Courses/QBIO7006_Assignment_1")
# Load data -----
data <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
### Assignment 1: Tidy data package
# Load library ------
library(tidyverse)
library(ggplot2)
# Load data -----
data <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
### Assignment 1: Tidy data package
# Load library ------
library(tidyverse)
library(ggplot2)
# Load data -----
rm(list=ls())
data <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
View(data)
colnames(data)
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
colnames(rawData) <- c("minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
### Assignment 1: Tidy data package
# Load library ------
library(tidyverse)
library(ggplot2)
# Load data -----
rm(list=ls())
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
function (..., list = character(), package = NULL, lib.loc = NULL,
verbose = getOption("verbose"), envir = .GlobalEnv, overwrite = TRUE)
colnames(rawData) <- c("minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
View(rawData)
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
View(rawData)
View(rawData)
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))
tidyData$minTemp <- gsub("([0-9]+)\D*","\\1", tidyData$minTemp)
tidyData$minTemp <- gsub("([0-9]+)\\D*","\\1", tidyData$minTemp)
tmp <- apply(tidyData$minTemp, function(x) gsub("([0-9]+)\\D*","\\1", x))
tmp <- lapply(tidyData$minTemp, function(x) gsub("([0-9]+)\\D*","\\1", x))
tmp <- sapply(tidyData$minTemp, function(x) gsub("([0-9]+)\\D*","\\1", x))
tidyData[] <- lapply(tidyData, function(x) sub("([0-9]+)\\D*","\\1", x))
gsub("([0-9]+)\\D*","\\1", text)
text <- "25.4 �C"
gsub("([0-9]+)\\D*","\\1", text)
text <- "25.4 �C"
gsub("([0-9]+)\\d*","\\1", text)
gsub("([0-9]+[.][0-9]+)\\D*","\\1", text)
tidyData[] <- lapply(tidyData, function(x) gsub("([0-9]+[.][0-9]+)\\D*","\\1", text))
View(tidyData)
# Load data -----
rm(list=ls())
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))
tidyData[] <- lapply(tidyData, function(x) gsub("([0-9]+[.][0-9]+)\\D*","\\1", x))
?lapply
tidyData$minTemp <- lapply(tidyData$minTemp, function(x) gsub("([0-9]+[.][0-9]+)\\D*","\\1", x))
tidyData$minTemp <- lapply(as.string(tidyData$minTemp), function(x) gsub("([0-9]+[.][0-9]+)\\D*","\\1", x))
tidyData$minTemp <- lapply(as.str(tidyData$minTemp), function(x) gsub("([0-9]+[.][0-9]+)\\D*","\\1", x))
tidyData$minTemp <- lapply(as.character(tidyData$minTemp), function(x) gsub("([0-9]+[.][0-9]+)\\D*","\\1", x))
class(tidyData$minTemp)
tidyData$minTemp[1]
tidyData$minTemp[0]
tidyData$minTemp[2]
View(tidyData)
text <- "11.3 \xb0C"
gsub("([^0-9.]+)","\\1", text)
text <- "11.3 \xb0C"
gsub("[^0-9.]+","" text)
text <- "11.3 \xb0C"
gsub("[^0-9.]+","", text)
gsub("([^0-9.]+[0-9]+)\s\w*","\\1", text)
gsub("([^0-9.]+[0-9]+)\t\w*","\\1", text)
gsub("^([^ ]+).*","\\1", text)
text <- "11.3 \xb0C"
gsub("^([^ ]+).*","\\1", text)
text <- as.str("11.3 \xb0C")
text <- as.character("11.3 \xb0C")
text
gsub("^([^ ]+).*","\\1", text)
text <- "11.3 \xb0C"
gsub("([^0-9.]+)\\w*","\\1", text)
gsub("([^0-9.]+)\\t\\w*","\\1", text)
gsub("([^0-9.]+)\t\\w*","\\1", text)
text <- "11.3 \xb0C"
gsub("([^0-9.]+)\t\\w*","\\1", text)
text <- "11.3 \xb0C"
gsub("([^0-9.]+)\t\w*","\\1", text)
gsub("([^0-9.]+)\t.*","\\1", text)
gsub("([^0-9.]+).*","\\1", text)
gsub("(\\d+)\\s[:alnum:]", "\\1", text)
?gsub
text <- "11.3 \xb0C"
gsub("(\\d+\\.\\d+)\\s[:alnum:]", "\\1", text)
text <- as.character("11.3 \xb0C")
gsub("(\\d+\\.\\d+)\\s[:alnum:]", "\\1", text)
?strsplit()
strsplit(text," ")
strsplit(text," ")[[1]]
text <- as.character("11.3 \xb0C")
strsplit(text," ")
strsplit(text," ", useBytes = TRUE)
tidyData$minTemp <- lapply(tidyData$minTemp, function(x) strsplit(x," ", useBytes = TRUE) )
View(tidyData)
text <- as.character("11.3 \xb0C")
strsplit(text," ", useBytes = TRUE)
strsplit(text," ", useBytes = TRUE)[[1]][1]
tidyData$minTemp <- lapply(tidyData$minTemp, function(x) strsplit(x," ", useBytes = TRUE)[[1]][1])
View(tidyData)
text <- as.character("11.3 \xb0C")
tmp<-strsplit(text," ", useBytes = TRUE)[[1]][1]
tidyData$minTemp <- lapply(tidyData$minTemp, function(x) int(strsplit(x," ", useBytes = TRUE)[[1]][1]))
tidyData$minTemp <- lapply(tidyData$minTemp, function(x) as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1]))
tidyData$minTemp <- lapply(tidyData$minTemp, function(x) as.numeric(strsplit(x," ", useBytes = TRUE)[[1]]))
tidyData$minTemp <- lapply(tidyData$minTemp, function(x){
strsplit(text," ", useBytes = TRUE)[[1]][1]
})
View(tidyData)
tidyData$minTemp <- lapply(tidyData$minTemp, function(x){
strsplit(x," ", useBytes = TRUE)[[1]][1]
})
View(tidyData)
# Load data -----
rm(list=ls())
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))
View(tidyData)
tidyData$minTemp <- lapply(tidyData$minTemp, function(x){
x<-as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1])
})
View(tidyData)
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))
tidyData$getMinTemp <- lapply(tidyData$minTemp, function(x){
x<-as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1])
})
View(tidyData)
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))
tidyData$a <- lapply(tidyData$minTemp, function(x){
x<-as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1])
})
View(tidyData)
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year")) %>%
mutate(across(c("minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")),
~ apply(., 2, function(x){
x<-as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1])
}))
convertDateData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))
tidyData <- convertDateData %>%
mutate(across(4:, lapply(., function(x){
View(convertDateData)
tidyData <- convertDateData %>%
mutate(across(4:9, lapply(., function(x){
x<-as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1])
})))
tidyData <- convertDateData %>%
mutate(across(c("minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint"), lapply(., function(x){
x<-as.numeric(strsplit(x," ", useBytes = TRUE)[[1]][1])
})))
tidyData <- convertDateData %>%
mutate(across(c("minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint"), ~ as.numeric(strsplit(.," ", useBytes = TRUE)[[1]][1])))
View(tidyData)
# Load data -----
rm(list=ls())
rawData <- read_csv("data/PJ Sun 1 tiny tag 1 daily min max.csv")
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))  %>%
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(strsplit(.," ", useBytes = TRUE)[[1]][1])))
View(tidyData)
View(tidyData)
tidyData <- rawData %>%
separate(Date, into = c("Day", "Month", "Year"))  %>%
mutate(across(starts_with("min") | starts_with("max"),
~ sapply(strsplit(., " ", useBytes = TRUE), function(x) x[1])))
View(tidyData)
?sapply
View(tidyData)
str(tidyData)
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1]))))
str(tidyData)
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
View(tidyData)
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(cols = 4:9,
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
head(rawData)
}
View(rawData)
View(tidyData)
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
summary(rawData)
}
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- bind_cols(resultData, tidyData)
}
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- cbinds(resultData, tidyData)
}
### Assignment 1: Tidy data package
# Load library ------
library(tidyverse)
library(ggplot2)
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- cbinds(resultData, tidyData)
}
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- cbind(resultData, tidyData)
}
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- bind_cols(resultData, tidyData)
}
dataName = nameList[2]
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
View(tidyData)
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
dataName = nameList[2]
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- bind_cols(tidyData, resultData)
}
dataName = nameList[2]
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultData <- data.frame()
for (dataName in nameList) {
dataName = nameList[2]
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultData <- bind_cols(resultData, tidyData)
}
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultList <- list()
for (dataName in nameList) {
dataName = nameList[2]
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultList <- c(resultList, tidyData)
}
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultList <- list()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultList <- c(resultList, tidyData)
}
View(resultList)
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultList <- list()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultList <- c(resultList, list(tidyData))
}
View(resultList)
resultData <- reduce(resultList, bind_cols)
View(resultList)
resultData <- reduce(resultList, bind_rows)
View(resultData)
### Assignment 1: Tidy data package
# Load library ------
library(tidyverse)
library(ggplot2)
# Load data -----
rm(list=ls())
nameList <- c("PJ Sun 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 1 daily min max", "PJ Shade 1 tiny tag 2 daily min max")
resultList <- list()
for (dataName in nameList) {
rawData <- read_csv(paste0("data/", dataName, ".csv"))
colnames(rawData) <- c("Date", "minTemp", "maxTemp", "minHumid", "maxHumid", "minDewPoint", "maxDewPoint")
tidyData <- rawData %>%
# Convert Date column into Day, Month, and Year
separate(Date, into = c("Day", "Month", "Year"))  %>%
# Except the first three columns (Day, Month, and Year),
# use strsplit to get only the number value from columns starting with "min" or "max"
mutate(across(starts_with("min") | starts_with("max"),
~ as.numeric(sapply(strsplit(as.character(.), " ", useBytes = TRUE), function(x) x[1])))) %>%
pivot_longer(cols = 4:9, names_to = "Measure", values_to = "Value")
resultList <- c(resultList, list(tidyData))
}
resultData <- reduce(resultList, bind_rows)
3282+2730+3282
